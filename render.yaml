services:
  - type: web
    name: simple-chat-server
    env: node
    plan: free
    region: oregon
    branch: main
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
